using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraScript : MonoBehaviour
{
    //public Transform target;
    //public float cameraSpeed = 0.15f;
    //public Vector3 offset;
    //private Vector3 smoothedPosition;

    //private void LateUpdate()
    //{
    //    Vector3 desiredPosition = target.position + offset;
    //    transform.position = Vector3.Lerp(transform.position, desiredPosition, cameraSpeed);
    //    transform.position = smoothedPosition;

    ////Makes the camera follow the target (balls) at an offset, so the camera is not on the ball, it follows it from an offset 
    
    //}


    //This was my attempt at the Camera Movement criteria.
    //The camera follows the target, however, given that the ball changes, I couldn't figure out how to make the target change
    //each time the ball changed.
}
